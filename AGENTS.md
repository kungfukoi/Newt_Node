# NewtNode Agent Instructions

Start with `docs/README.md`, then read `docs/node-standards.md` before adding or changing a feature. Follow the standards document as the current source of truth for node behavior, UI conventions, backend routes, media persistence, workflow packages, progress, stats, and verification.

If a change deliberately updates those conventions, update `docs/node-standards.md` in the same change.
